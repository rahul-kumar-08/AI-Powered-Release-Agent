# MCP client library (requires Python 3.10+)
fastmcp>=2.0.0

# agent_runner.py uses the Cursor SDK for mission decomposition.
cursor-sdk>=0.1.0

# SFTP upload for changelog/rpm artifacts
paramiko>=3.0.0

# Tabular output rendering (release tables, MCP token validation status, agent_runner summaries).
# pandas.DataFrame.to_markdown() requires the tabulate backend.
pandas>=2.0.0
tabulate>=0.9.0
